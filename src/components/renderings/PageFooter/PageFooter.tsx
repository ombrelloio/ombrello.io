import { Logo, Link, RichText } from "@atoms";
import { Container, Row, Col } from "@layout";

type PageFooterProps = {};

const PageFooter = () => (
  <footer className="sticky top-[100vh] mt-12 md:mt-24">Footer</footer>
);

export default PageFooter;
